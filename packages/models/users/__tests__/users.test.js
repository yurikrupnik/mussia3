// 'use strict';

// const users = require('..');
import fs from '../src/index';

describe('users', () => {
    it('needs tests', () => {
        // const user = new I
        fs();
        expect(1).toBe(1);
    });
});
